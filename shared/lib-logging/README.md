# Shared Logging Library

Provides structured logging, Prometheus metrics, and distributed tracing for all microservices.

## Installation

```bash
pip install -r requirements.txt
```

## Usage

See `src/logger.py`, `src/metrics.py`, and integration with `lib-tracing`.
